import 'package:flutter/material.dart';

class AppColor {
  static const Color primary =  Color.fromRGBO(
      76, 126, 210, 0.75);
  static const Color background = Colors.white;

  static const Color foreground = Color.fromRGBO(
      76, 126, 210, 0.75);
  static final Color appdesktop = Colors.grey[100]!;

}
